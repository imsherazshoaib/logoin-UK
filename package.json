{
  "name": "logoinusa",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "parcel src/pages/**/*.html",
    "lint": "eslint src/js/**/*.js",
    "format": "prettier src/**/*.{js,html,css} --write",
    "build": "parcel build ./src/**/*.html && cp ./src/CNAME ./dist",
    "deploy": "npm run build && surge dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fortawesome/fontawesome-free": "^5.13.0",
    "bootstrap": "^4.4.1"
  },
  "devDependencies": {
    "eslint": "^6.8.0",
    "parcel": "^1.12.4",
    "prettier": "^2.0.5"
  }
}
